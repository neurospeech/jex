const coreLogger = {
    debug(...a) {
        console.log(...a);
    },
    info(...a) {
        console.log(...a);
    },
    warning(...a) {
        console.warn(...a);
    }
};

export default coreLogger;